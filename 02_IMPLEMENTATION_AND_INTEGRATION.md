# Workflow 2 — 계획 수립 + 구현 + 검증·리뷰 + 통합

> 상태: 초안 0.3
>
> 목적: 승인된 요구사항과 설계를 구현하고, 완료 여부를 검증하여 안전하게 통합한다.

## 1. 시작 조건

다음 입력이 준비되어 있어야 한다.

- 사용자가 승인한 요구사항 명세
- 사용자가 승인한 SW 설계
- ADR과 기술적 제약
- 측정 가능한 인수 조건
- 확인 가능한 저장소의 현재 상태

선행 단계: [Workflow 1 — 요구사항 정의 + SW 설계](./01_REQUIREMENTS_AND_DESIGN.md)

승인된 기준선이 없거나 현재 저장소와 명백히 충돌하면 구현을 시작하지 않는다. 먼저 Workflow 1을 보완하고 사용자 승인을 받는다.

예외: [Workflow 1의 적용 제외 — 경량 경로](./01_REQUIREMENTS_AND_DESIGN.md#적용-제외--경량-경로) 조건을 만족하는 작업은 요구사항 명세와 설계 문서 없이 시작할 수 있다. 이 경우에도 변경 범위 준수, 검증, 결과 보고는 생략하지 않는다.

## 2. 기본 원칙

### 2.1 기준선

사용자가 승인한 요구사항과 설계를 작업의 기준선으로 삼는다. 계획, 구현, 검증은 기준선의 관련 항목을 추적할 수 있어야 한다.

### 2.2 변경 범위

- 요청과 승인된 설계에 필요한 변경만 수행한다.
- 사용자가 만든 기존 변경과 관련 없는 파일을 보존한다.
- 범위 밖에서 발견한 문제는 임의로 수정하지 않고 별도 항목으로 보고한다.
- 새로운 요구사항이나 핵심 설계 변경을 구현 세부사항으로 위장하지 않는다.

### 2.3 자율 진행과 승인

다음 조건을 모두 만족하면 AI는 계획 수립 후 별도 승인 없이 구현을 계속할 수 있다.

- 승인된 요구사항과 설계 안에서 수행되는 변경
- 되돌릴 수 있는 로컬 변경
- 공개 인터페이스, 데이터 모델, 보안 정책을 중대하게 바꾸지 않는 변경
- 외부 시스템에 상태 변경을 일으키지 않는 작업

다음 작업은 사용자가 명시적으로 요청하거나 승인한 경우에만 수행한다.

- 원격 저장소에 push
- Pull Request 생성 또는 병합
- 패키지 게시
- 운영 환경 배포
- 외부 사용자나 시스템에 결과 전송
- 데이터 삭제와 되돌리기 어려운 마이그레이션

### 2.4 완료의 증거

- “완료”는 코드가 작성되었다는 뜻이 아니다.
- 완료 여부는 인수 조건과 검증 결과로 판단한다.
- 실행하지 못한 검증은 성공한 것으로 간주하지 않는다.
- 검증하지 못한 항목과 남은 위험을 최종 결과에 명시한다.

## 3. 진행 절차

### 3.1 현재 상태 재확인

AI는 계획을 작성하기 직전에 다음을 확인한다.

- 이전 세션에서 진행하던 작업 기록(`docs/work/<작업-ID>/`)이 있는가? 있다면 계획의 진행 상태와 미완료 항목부터 확인한다.
- 기준선 작성 이후 저장소가 변경되었는가?
- 사용자 또는 다른 작업의 미커밋 변경이 있는가?
- 관련 테스트와 빌드가 현재 어떤 상태인가?
- 요구사항과 설계가 실제 코드 구조에 여전히 적용 가능한가?

중대한 차이가 있으면 영향 범위를 분석하고 Workflow 1을 보완한다.

### 3.2 계획 수립

계획에는 다음 내용을 포함한다.

- 작업 단위와 수행 순서
- 각 작업이 충족할 요구사항과 설계 요소
- 수정하거나 추가할 주요 파일
- 작업 간 의존성
- 테스트와 검증 방법
- 위험이 높은 변경과 선행 확인 사항
- 데이터 마이그레이션 및 롤백 방법
- 문서와 운영 환경 변경

각 작업은 하나의 명확한 결과와 완료 조건을 가진다. 가능한 경우 독립적으로 구현하고 검증할 수 있는 크기로 나눈다.

권장 작업 형식:

```markdown
## 작업명

- 목표:
- 관련 요구사항:
- 관련 설계:
- 변경 대상:
- 의존성:
- 위험:
- 검증 방법:
- 완료 조건:
```

계획의 진행 상태는 작업 중 갱신한다.

```text
pending → in progress → completed
```

동시에 여러 작업을 진행하더라도 한 작업의 책임과 결과가 모호해지지 않도록 한다.

계획 자체가 새로운 제품 결정이나 중대한 설계 결정을 포함하면 구현 전에 사용자 승인을 받는다.

### 3.3 구현

AI는 계획의 순서에 따라 작은 단위로 구현한다.

- 관련 코드와 테스트를 함께 변경한다.
- 저장소의 기존 스타일과 구조를 따른다.
- 사용자 변경과 관련 없는 변경을 보존한다.
- 임시 우회책, 미완료 사항, 기술 부채를 숨기지 않는다.
- 공개 인터페이스, 설정, 마이그레이션이 바뀌면 관련 문서를 갱신한다.
- 각 작업 완료 시 가장 가까운 범위의 검증을 수행한다.

구현 중 설계와 다른 선택이 필요한 경우 다음을 기록한다.

- 설계대로 구현할 수 없는 이유
- 영향을 받는 요구사항과 인수 조건
- 가능한 대안
- 권장 변경과 위험

단순한 구현 세부사항이면 계획과 문서를 갱신하고 계속한다. 요구사항이나 핵심 설계를 바꾸는 내용이면 해당 구현을 보류하고 DCR 및 ADR 초안을 작성한 뒤 [Workflow 1의 승인된 설계 변경 절차](./01_REQUIREMENTS_AND_DESIGN.md#5-승인된-설계-변경)로 돌아간다.

Workflow 1에서 새 설계 기준선이 승인되면 다음을 구분하여 계획에 반영한다.

- 그대로 유지할 구현
- 새 설계에 맞게 수정할 구현
- 되돌릴 구현
- 추가하거나 변경할 검증
- 마이그레이션 및 롤백 작업

### 3.4 검증

검증은 변경 위험에 비례하여 다음 순서로 수행한다.

1. 포맷 및 정적 분석
2. 변경 영역의 단위 테스트
3. 컴포넌트 간 통합 테스트
4. 인수 조건 기반 시나리오 테스트
5. 성능, 보안, 호환성 등 관련 비기능 검증
6. 빌드 또는 실제 실행 확인

각 인수 조건에 대해 다음을 기록한다.

| 인수 조건 | 검증 방법 | 결과 | 증거 |
|---|---|---|---|
| AC-01 | 테스트 또는 확인 절차 | 성공·실패·미수행 | 로그, 테스트명, 실행 결과 |

테스트 실패는 다음과 같이 구분한다.

- 이번 변경으로 발생한 실패
- 기존에도 존재한 실패
- 실행 환경 또는 외부 의존성으로 인한 실패
- 원인을 확인하지 못한 실패

기존 실패라고 판단하면 근거와 재현 방법을 남긴다. 실패한 검증을 단순히 제외하여 전체 성공으로 보고하지 않는다.

검증 결과와 증거 요약(실행한 명령, 테스트 이름, 핵심 출력)은 작업 기록의 검증 섹션에 남긴다. 대화에만 남긴 검증 결과는 세션이 끝나면 추적할 수 없다.

### 3.5 자체 리뷰

AI는 구현 완료 후 전체 변경 사항을 다시 검토한다.

#### 요구사항과 설계

- 요구사항과 인수 조건을 모두 충족하는가?
- 승인된 설계와 구현이 일치하는가?
- 설계와 달라진 부분이 기록되었는가?
- 불필요하거나 범위 밖인 변경이 포함되지 않았는가?

#### 정확성과 안정성

- 오류 처리와 경계 조건이 충분한가?
- 실패 시 데이터와 상태가 일관되게 유지되는가?
- 재시도, 동시성, 중복 실행 문제가 없는가?
- 기존 동작에 의도하지 않은 회귀가 없는가?

#### 보안과 운영

- 인증, 권한, 입력 검증이 적절한가?
- 비밀 정보나 개인정보가 노출되지 않는가?
- 데이터 손실 또는 하위 호환성 위험이 없는가?
- 로그와 오류 메시지가 운영에 충분하고 안전한가?

#### 품질

- 테스트가 구현 방식이 아니라 요구 동작을 검증하는가?
- 코드가 기존 구조와 스타일에 맞는가?
- 문서와 코드가 일치하는가?
- 디버그 코드, 비밀 정보, 임시 파일이 남지 않았는가?

리뷰에서 발견한 문제는 수정하고 영향을 받는 검증을 다시 수행한다.

### 3.6 통합

통합은 구현 결과를 저장소와 실제 사용 흐름에 완결된 상태로 반영하는 과정이다.

- 관련 코드, 테스트, 문서, 설정을 일관되게 갱신한다.
- 필요한 마이그레이션과 롤백 절차를 준비한다.
- 통합된 상태에서 최종 빌드와 테스트를 수행한다.
- 요구사항과 인수 조건의 충족 여부를 최종 확인한다.
- 남은 제한 사항과 후속 작업을 기록한다.

로컬 작업 트리에 변경 사항을 완결된 형태로 반영하는 것은 기본 작업 범위에 포함한다. 커밋, push, PR, 병합, 배포는 사용자의 요청 범위에 따라 수행한다.

## 4. 변경 관리

### 4.1 경미한 변경

다음 변경은 AI가 이유를 기록하고 현재 워크플로우 안에서 계속 진행할 수 있다.

- 내부 구현 세부사항
- 승인된 설계를 더 명확히 하는 문서 보완
- 인수 조건의 의미를 바꾸지 않는 테스트 보강
- 공개 계약에 영향을 주지 않는 코드 구조 개선

### 4.2 중대한 변경

다음 변경은 영향받는 구현을 보류하고 DCR 및 ADR 초안을 작성한 뒤 Workflow 1의 승인된 설계 변경 절차에 따라 사용자의 재승인을 받는다.

- 제품 동작이나 범위 변경
- 인수 조건 변경
- 공개 인터페이스 또는 데이터 모델의 중대한 변경
- 주요 컴포넌트의 책임 변경
- 새로운 보안 또는 개인정보 위험
- 비용, 배포, 호환성 위험 증가
- 기존 ADR을 뒤집는 결정
- 되돌리기 어려운 데이터 마이그레이션

## 5. 완료 조건

다음 조건을 모두 만족하면 작업이 완료된다.

- 계획된 작업이 모두 완료되었거나 제외 이유가 기록되었다.
- 구현이 승인된 요구사항과 설계에 일치한다.
- 각 인수 조건의 검증 결과가 기록되었다.
- 관련 자동화 테스트와 빌드가 성공했다.
- 실패하거나 수행하지 못한 검증이 명시되었다.
- 자체 리뷰에서 발견된 중대한 문제가 해소되었다.
- 코드, 테스트, 문서, 설정이 함께 통합되었다.
- 남은 위험, 제한 사항, 후속 작업이 공개되었다.

조건을 충족하지 못한 상태에서 작업을 중단해야 한다면 “완료”가 아니라 “부분 완료” 또는 “차단됨”으로 보고한다.

부분 완료 또는 차단 상태로 중단할 때는 작업 기록에 다음을 남긴다.

- 중단 사유
- 보존한 변경과 그 상태
- 되돌린 변경과 그 이유
- 재개 조건과 다음 단계

## 6. 최종 결과 보고

AI는 다음 형식으로 작업 결과를 보고한다.

```markdown
## 작업 결과

- 완료 상태: 완료 | 부분 완료 | 차단됨
- 완료한 내용:
- 주요 변경:
- 설계와 달라진 점:
- 검증 결과:
- 인수 조건 충족 여부:
- 통합 상태:
- 남은 위험 또는 제한:
- 후속 작업:
```

검증 결과에는 성공 여부뿐 아니라 실제로 실행한 명령, 테스트 또는 확인 방법을 포함한다.

## 7. 작업 기록 형식

작업 기록은 `docs/work/<작업-ID>/`에 유지한다. 계획의 진행 상태와 검증 결과는 대화가 아니라 이 기록에 갱신하며, 세션이 중단된 뒤 재개할 때 가장 먼저 읽는 재개 지점으로 사용한다. 경량 경로 작업은 기록을 생략하거나 축약할 수 있다.

권장 구조:

```markdown
# 작업명

## 기준선

- 관련 요구사항:
- 관련 설계:
- 관련 ADR:

## 작업 정의

- 목표:
- 범위:
- 범위 밖:
- 가정:
- 위험:

## 계획

- [ ] 작업 1
- [ ] 작업 2
- [ ] 작업 3

## 검증

- 검증 방법:
- 기대 결과:
- 실제 결과:

## 완료

- 변경 요약:
- 인수 조건 충족 여부:
- 남은 사항:
```
