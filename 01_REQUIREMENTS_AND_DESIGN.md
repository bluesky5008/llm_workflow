# Workflow 1 — 요구사항 정의 + SW 설계

> 상태: 초안
>
> 목적: 사용자의 요청을 구현 가능한 요구사항과 승인 가능한 SW 설계로 구체화한다.

## 1. 적용 시점

다음 상황에서 이 워크플로우를 사용한다.

- 새로운 기능이나 시스템을 개발할 때
- 기존 기능의 동작이나 공개 인터페이스를 변경할 때
- 요구사항 또는 기술적 접근 방법이 명확하지 않을 때
- 데이터 모델, 보안 정책, 아키텍처를 변경할 때
- 승인된 설계가 없거나 현재 저장소와 일치하지 않을 때

이 단계에서는 원칙적으로 제품 코드를 변경하지 않는다. 저장소 조사, 문서 작성, 읽기 전용 분석은 수행할 수 있다.

설계의 실현 가능성을 확인하기 위한 프로토타입이 필요하면 다음을 먼저 명시한다.

- 검증하려는 가설
- 프로토타입의 범위
- 제품 코드에 포함할지 폐기할지 여부
- 성공과 실패를 판단할 기준

## 2. 입력

- 사용자의 작업 요청
- 문제의 배경과 기대 결과
- 기존 저장소, 문서, 실행 환경
- 일정, 기술, 운영상의 제약
- 사용자가 이미 내린 결정

입력이 불완전해도 저장소와 실행 환경에서 확인 가능한 부분부터 조사한다. 결과를 크게 바꾸는 정보만 사용자에게 질문한다.

## 3. 기본 원칙

### 3.1 사실과 불확실성

- 확인한 사실, 해석, 가정을 구분한다.
- 직접 확인할 수 있는 내용은 사용자에게 묻기 전에 조사한다.
- 확인할 수 없는 내용은 추측하지 않고 가정 또는 미해결 질문으로 기록한다.
- 결과를 크게 바꾸지 않는 낮은 위험의 가정은 명시한 뒤 진행할 수 있다.
- 보안, 데이터, 비용, 공개 인터페이스 또는 호환성에 영향을 주는 가정은 사용자에게 확인한다.

### 3.2 범위

- 해결할 문제와 성공 기준을 먼저 정의한다.
- 범위와 범위 밖 항목을 함께 기록한다.
- 요청하지 않은 기능을 설계에 임의로 추가하지 않는다.
- 기존 시스템과 사용자 변경을 제약 조건으로 취급한다.

### 3.3 결정

- 중요한 결정은 선택 결과뿐 아니라 배경과 근거를 기록한다.
- 여러 대안이 의미 있게 경쟁한다면 장단점을 비교한다.
- 오랫동안 영향을 주거나 되돌리기 어려운 결정은 ADR로 분리한다.
- 제품의 목표, 범위 및 중대한 기술 결정은 사용자가 승인한다.

## 4. 진행 절차

### 4.1 현재 상태 조사

AI는 요구사항과 설계를 확정하기 전에 다음을 확인한다.

- 저장소 구조와 적용되는 개발 지침
- 현재 동작과 재현 방법
- 관련 모듈, 데이터, 인터페이스
- 기존 테스트와 문서
- 작업 중인 사용자 변경
- 기술적 제약과 잠재적 영향 범위

조사 결과에는 다음을 구분하여 기록한다.

- 확인된 사실
- 사실로부터 도출한 해석
- 아직 확인하지 못한 가정
- 사용자 결정이 필요한 질문

### 4.2 요구사항 정의

다음 내용을 구체화한다.

- 해결할 문제와 목적
- 대상 사용자 또는 호출 주체
- 주요 사용 시나리오
- 기능 요구사항
- 비기능 요구사항
- 범위와 범위 밖 항목
- 제약 조건과 의존성
- 예외 상황과 실패 시 기대 동작
- 측정 가능한 인수 조건

요구사항은 추적할 수 있도록 식별자를 붙이는 것을 권장한다.

```text
FR-01   기능 요구사항
NFR-01  비기능 요구사항
AC-01   인수 조건
```

좋은 인수 조건은 다음 요소를 포함한다.

- 입력 또는 시작 상황
- 수행할 행동이나 발생한 사건
- 관찰 가능한 기대 결과

구현 방법을 요구사항으로 고정하지 않는다. 특정 기술이 반드시 필요하다면 그 이유와 제약을 함께 기록한다.

### 4.3 SW 설계

요구사항을 만족하기 위해 다음 항목을 필요한 수준까지 설계한다.

#### 시스템 구조

- 시스템 경계와 외부 연동
- 아키텍처와 주요 컴포넌트
- 컴포넌트별 책임
- 컴포넌트 간 의존 관계

#### 데이터와 인터페이스

- 데이터 모델과 상태
- 저장 및 수명 주기
- API, 이벤트, 메시지, 파일 형식 등의 계약
- 입력 검증과 오류 표현
- 버전 및 하위 호환성

#### 동작

- 정상 시나리오
- 경계 및 예외 시나리오
- 실패 감지와 복구
- 재시도, 중복 처리, 타임아웃
- 동시성 및 데이터 일관성

#### 품질 속성

- 인증과 권한
- 비밀 정보와 개인정보 보호
- 성능과 자원 사용
- 안정성과 가용성
- 확장성과 유지보수성
- 로그, 지표, 추적, 알림

#### 전환과 검증

- 기존 시스템과의 호환성
- 데이터 또는 설정 마이그레이션
- 롤백 방법
- 테스트 가능성
- 요구사항 및 인수 조건별 검증 전략

모든 항목을 기계적으로 작성할 필요는 없다. 작업에 영향을 주는 항목을 선택하되, 제외한 중요한 항목이 있다면 제외 이유를 기록한다.

### 4.4 설계 결정 기록

중요한 기술 결정에는 다음을 기록한다.

- 결정이 필요한 배경
- 결정에 영향을 주는 요구사항과 제약
- 고려한 대안
- 선택한 안과 이유
- 장점과 감수할 단점
- 영향 범위

ADR은 다음과 같은 결정에 사용한다.

- 핵심 기술이나 데이터 저장 방식 선택
- 주요 컴포넌트의 책임과 경계
- 외부 인터페이스 형식
- 인증, 권한, 개인정보 처리 방식
- 되돌리기 어렵거나 다른 작업에 장기간 영향을 주는 결정

ADR의 권장 형식:

```markdown
# ADR-001: 결정 제목

- 상태: 제안 | 승인 | 거부 | 폐기 | 대체됨
- 날짜:
- 관련 변경 요청:
- 대체 대상 ADR: 이 ADR이 대체하는 기존 ADR
- 대체 ADR: 이 ADR을 대체한 후속 ADR

## 배경

## 고려한 대안

## 결정

## 이유

## 결과와 단점
```

### 4.5 일관성 검토

AI는 사용자에게 승인을 요청하기 전에 자체 검토한다.

- 모든 핵심 요구사항이 하나 이상의 설계 요소에 연결되는가?
- 모든 인수 조건을 검증할 방법이 있는가?
- 설계가 범위 밖 기능을 불필요하게 포함하지 않는가?
- 컴포넌트의 책임과 인터페이스가 모호하지 않은가?
- 주요 실패와 복구 시나리오가 빠지지 않았는가?
- 보안 및 데이터 위험이 식별되었는가?
- 기존 동작과의 호환성 영향이 설명되었는가?
- 구현 작업으로 분해할 만큼 설계가 구체적인가?

## 5. 승인된 설계 변경

이 절차는 Workflow 1에서 이미 승인된 설계를 Workflow 2의 구현 중 발견한 사실이나 ADR 제안에 따라 변경할 때 사용한다.

구현 중 발견된 설계 변경은 자동으로 승인된 결정이 아니다. Workflow 2에서 작성한 ADR은 사용자 승인 전까지 **제안 상태**이며, 이 절차를 완료해야 새로운 설계 기준선에 포함된다.

### 5.1 변경 원칙

- 현재 설계 문서는 최신 승인 상태를 설명하는 기준 문서로 유지한다.
- 승인된 설계를 근거와 기록 없이 덮어쓰지 않는다.
- 변경 전 설계는 버전 관리 이력과 설계 변경 기록으로 추적할 수 있어야 한다.
- 승인된 ADR의 결정과 근거 본문은 과거 기록이므로 새 결론에 맞춰 고쳐 쓰지 않는다.
- 기존 ADR을 변경해야 하면 새 ADR을 만들고 두 ADR의 대체 관계를 기록한다.
- 변경의 영향을 받는 구현은 재승인 전까지 확정하거나 통합하지 않는다.
- 영향받지 않는 작업은 의존 관계가 없음을 확인한 경우에만 계속할 수 있다.

### 5.2 변경 시작 조건

다음과 같은 상황이면 Workflow 2에서 설계 변경을 시작한다.

- 설계대로 구현할 수 없다는 기술적 근거가 발견됨
- 승인 당시의 가정이 실제 코드나 실행 결과와 다름
- 더 안전하거나 단순한 대안이 구현 과정에서 확인됨
- 공개 인터페이스, 데이터 모델 또는 컴포넌트 책임을 바꿔야 함
- 새로운 보안, 개인정보, 성능 또는 운영 위험이 발견됨
- 기존 ADR을 뒤집거나 보완해야 함
- 이미 구현한 일부가 승인된 설계와 다름

단순한 코드 배치, 함수명, 내부 리팩터링처럼 설계의 의미와 계약을 바꾸지 않는 사항은 Workflow 2의 경미한 변경으로 처리할 수 있다.

### 5.3 변경 절차

#### A. 영향 범위의 구현 보류

AI는 설계 변경의 영향을 받는 구현과 통합을 보류한다. 이미 작성한 코드는 삭제하거나 정당화하지 않고 현재 상태를 보존한 채 다음을 기록한다.

- 완료된 구현
- 진행 중인 구현
- 실험 또는 임시 구현
- 아직 시작하지 않은 작업
- 변경 시 유지, 수정 또는 되돌려야 할 부분

보안 사고나 데이터 손실을 막기 위한 긴급 조치는 먼저 수행할 수 있다. 이 경우 조치 내용과 사후 설계 변경이 필요한 이유를 기록한다.

#### B. 설계 변경 요청 작성

각 변경에는 `DCR`(Design Change Request) 식별자를 부여한다.

```text
DCR-001
```

설계 변경 요청에는 다음을 포함한다.

```markdown
# DCR-001: 변경 제목

- 상태: 제안 | 승인 | 거부 | 보류 | 반영 완료
- 발견 위치: Workflow 2의 작업 또는 구현 항목
- 현재 설계 기준선:
- 관련 요구사항:
- 관련 ADR:

## 변경 사유와 증거

## 기존 설계

## 제안 설계

## 변경 항목

## 영향 범위

## 구현된 코드의 처리

## 마이그레이션 및 롤백

## 검증 방법
```

`변경 항목`에는 최소한 기존 내용, 변경할 내용, 변경 이유를 기록한다. 코드, 테스트 또는 실행 결과에서 발견한 근거가 있다면 재현 방법과 함께 연결한다.

#### C. 변경 분류

변경을 다음과 같이 분류한다.

- **명확화:** 설계의 의미나 외부 동작을 바꾸지 않고 모호한 설명만 보완
- **설계 변경:** 구조, 책임, 인터페이스, 데이터, 품질 속성 또는 기존 ADR의 결정을 변경
- **요구사항 변경:** 제품 동작, 범위 또는 인수 조건까지 변경

명확화는 설계 변경 기록을 남기고 문서를 갱신할 수 있다. 설계 변경과 요구사항 변경은 사용자 재승인을 받아야 한다. 요구사항 변경이면 요구사항 명세와 인수 조건부터 함께 갱신한다.

#### D. 대안 및 영향 분석

다음 항목을 변경 전후로 비교한다.

- 관련 요구사항과 인수 조건
- 컴포넌트 책임과 의존 관계
- 공개 및 내부 인터페이스
- 데이터 모델과 기존 데이터
- 정상, 실패, 복구 흐름
- 보안, 성능, 안정성, 운영성
- 호환성, 마이그레이션, 롤백
- 테스트 및 관측 방법
- 완료되었거나 진행 중인 구현
- 다른 ADR과 후속 작업

의미 있는 대안이 둘 이상이면 장단점, 위험, 전환 비용을 비교한다. 증거가 부족한 핵심 가정은 제한된 프로토타입이나 테스트로 먼저 검증한다.

#### E. ADR 제안 또는 대체

설계 변경이 중요한 기술 결정을 포함하면 ADR을 작성한다.

- 새로운 결정은 새 ADR을 `제안` 상태로 작성한다.
- 기존 ADR을 변경하는 경우에도 새 ADR 번호를 사용한다.
- 새 ADR의 `대체 대상 ADR`에 이전 ADR을 기록한다.
- 사용자 승인 후 이전 ADR의 상태만 `대체됨`으로 바꾸고 `대체 ADR`에 새 ADR 링크를 추가한다.
- 이전 ADR의 배경, 대안, 결정, 이유, 결과 본문은 수정하지 않는다.
- 거부된 ADR은 삭제하지 않고 `거부` 상태와 이유를 남긴다.

이 방식으로 현재 유효한 결정과 결정이 바뀐 역사를 모두 추적한다.

#### F. 설계 문서 갱신

AI는 제안된 결정을 반영하여 설계 문서를 현재 상태 기준으로 갱신한다.

- 변경된 구조, 책임, 인터페이스, 데이터 및 흐름
- 변경된 품질 속성과 운영 방법
- 변경된 마이그레이션 및 롤백
- 변경된 검증 전략
- 관련 요구사항, ADR, DCR 링크

변경된 문장만 고치는 것으로 끝내지 않고 다이어그램, 예시, 계약, 실패 흐름 등 같은 결정을 표현하는 모든 부분의 일관성을 확인한다.

#### G. 일관성 및 회귀 검토

다음 항목을 확인한다.

- 변경 후에도 모든 요구사항과 인수 조건을 충족하는가?
- 변경과 관계없는 기존 요구사항이 손상되지 않는가?
- 새 ADR과 기존의 유효한 ADR이 충돌하지 않는가?
- 코드, 데이터, 외부 연동에 필요한 전환 방법이 있는가?
- 이미 구현한 부분의 유지, 수정, 되돌리기 판단이 명확한가?
- Workflow 2의 계획과 검증 항목을 어떻게 바꿀지 설명할 수 있는가?

### 5.4 설계 변경 기록

설계 문서에는 최신 승인 상태를 반영하고, 별도의 변경 기록에는 변경 이력을 누적한다.

| 변경 요청 | 날짜 | 변경 요약 | 관련 ADR | 이전 기준선 | 새 기준선 | 상태 |
|---|---|---|---|---|---|---|
| DCR-001 | YYYY-MM-DD | 변경 내용 | ADR-002 | v1 | v2 | 승인 |

설계 변경 기록은 최소한 다음을 추적해야 한다.

- 무엇이 바뀌었는가?
- 왜 바뀌었는가?
- 어떤 증거와 결정에 근거하는가?
- 누가 언제 승인했는가?
- 어느 기준선부터 유효한가?
- 구현과 검증에 어떤 후속 조치가 필요한가?

버전 관리 시스템을 사용하더라도 커밋 차이만을 설계 변경 기록으로 대신하지 않는다. 코드 차이는 변경 내용을 보여주지만 설계 변경의 이유와 승인 상태까지 설명하지는 않기 때문이다.

### 5.5 재승인과 새 기준선

AI는 다음 형식으로 사용자에게 재승인을 요청한다.

```markdown
## 설계 변경 승인 요청

- 변경 요청:
- 변경 사유와 증거:
- 기존 설계:
- 변경 설계:
- 관련 ADR:
- 영향받는 요구사항과 인수 조건:
- 구현된 코드의 처리:
- 마이그레이션 및 롤백:
- 검증 계획:
- 남은 위험:

승인 요청:
이 변경을 새 설계 기준선으로 승인할지 확인해 주세요.
```

사용자는 다음 중 하나로 응답한다.

- **승인:** 설계 문서와 ADR 상태를 확정하고 새 기준선을 발행
- **수정 요청:** 변경안을 보완한 뒤 다시 검토
- **거부:** 기존 설계를 유지하고 ADR 및 DCR에 거부 이유를 기록
- **보류:** 추가 증거나 의사결정을 확보할 때까지 영향 범위의 구현을 보류

승인 시 다음을 함께 수행한다.

1. DCR 상태를 `승인`으로 변경한다.
2. 새 ADR 상태를 `승인`으로 변경한다.
3. 대체된 ADR 상태와 상호 링크를 갱신한다.
4. 설계 문서의 버전 또는 기준선 식별자를 올린다.
5. 설계 변경 기록에 승인 결과를 추가한다.
6. 요구사항–설계–ADR–검증 추적 정보를 갱신한다.

관련 문서와 기록은 가능한 경우 동일한 커밋에서 변경하여 하나의 설계 기준선으로 식별할 수 있게 한다.

### 5.6 Workflow 2로 반환

새 기준선이 승인되면 Workflow 2에 다음 정보를 전달한다.

- 새 기준선과 변경된 문서
- 승인된 DCR 및 ADR
- 유지할 구현
- 수정할 구현
- 되돌릴 구현
- 추가하거나 변경할 테스트
- 마이그레이션 및 롤백 작업
- 재개할 계획 항목과 순서

Workflow 2는 기존 계획을 그대로 재개하지 않는다. 변경 영향을 반영하여 계획과 완료 조건을 갱신한 뒤 구현을 재개한다.

설계 변경은 다음 조건을 모두 만족하면 `반영 완료`로 기록한다.

- 새 기준선이 사용자에게 승인되었다.
- 관련 설계 문서와 ADR 상태가 일관되게 갱신되었다.
- Workflow 2의 계획과 검증 항목에 변경이 반영되었다.
- 구현 결과가 새 설계에 일치하는지 검증되었다.
- 필요한 마이그레이션과 통합이 완료되었다.

## 6. 산출물

기본 산출물은 다음과 같다.

- 요구사항 명세
- SW 설계 문서
- 미해결 질문과 가정
- 위험 목록
- 필요한 ADR
- 필요한 DCR 및 설계 변경 기록
- 요구사항–설계–검증 추적 정보

권장 저장 구조:

```text
docs/work/<작업-ID>/
├── requirements.md
├── design.md
├── design-change-log.md
├── changes/
│   └── DCR-001-<변경명>.md
└── decisions/
    └── ADR-001-<결정명>.md
```

작은 작업은 요구사항과 설계를 하나의 문서에 합칠 수 있다. 문서의 크기보다 필요한 결정과 근거가 빠지지 않는 것이 중요하다.

## 7. 완료 조건

다음 조건을 모두 만족하면 초안이 완료된다.

- 문제, 범위, 범위 밖 항목이 명확하다.
- 기능 및 비기능 요구사항이 정의되었다.
- 인수 조건이 관찰하고 검증할 수 있는 형태다.
- 핵심 요구사항과 설계의 연결을 설명할 수 있다.
- 주요 인터페이스, 데이터, 정상 및 실패 흐름이 정의되었다.
- 중요한 대안과 설계 결정의 근거가 기록되었다.
- 중대한 미해결 질문과 위험이 사용자에게 공개되었다.
- 구현 계획을 수립할 만큼 설계가 구체적이다.
- 기존 기준선의 변경인 경우 DCR, ADR 대체 관계, 변경 기록 및 새 기준선이 일관된다.

## 8. 사용자 승인 관문

AI는 다음 형식으로 결과를 보고한다.

```markdown
## 요구사항·설계 결과

- 목표:
- 범위:
- 핵심 요구사항:
- 설계 요약:
- 주요 결정:
- 가정:
- 남은 질문:
- 주요 위험:
- 산출물:

승인 요청:
이 요구사항과 설계를 구현 기준선으로 승인할지 확인해 주세요.
```

사용자는 다음 중 하나로 응답한다.

- **승인:** 현재 요구사항과 설계를 기준선으로 확정하고 Workflow 2로 이동
- **수정 요청:** 지정한 내용을 보완한 뒤 다시 검토
- **보류:** 미해결 정보나 의사결정을 확보할 때까지 중단

승인되지 않은 요구사항과 설계를 구현 기준으로 사용하지 않는다.

다음 단계: [Workflow 2 — 계획 수립 + 구현 + 검증·리뷰 + 통합](./02_IMPLEMENTATION_AND_INTEGRATION.md)
